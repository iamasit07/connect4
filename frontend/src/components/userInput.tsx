export const CreateGameInput = () => {
  return (
    <div>
      <input type="text" placeholder="Enter your username" />
      <button onClick={() => {}}>Create Game</button>
    </div>
  );
};

export const JoinGameInput = () => {
  return (
    <div>
      <input type="text" placeholder="Enter game ID or username" />
      <button onClick={() => {}}>Join Game</button>
    </div>
  );
};
